datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model UserStats {
  handle      String   @id
  solvedCount Int
  lastUpdated DateTime @default(now())
}
